# Environment variables for the backend container
# Copy to backend.env and adjust as needed before running docker-compose.

# Path to the model weights inside the container
MODEL_PATH=backup_best_model_20251015_170801.pth

# Port exposed by the Flask/Gunicorn app
PORT=5000
