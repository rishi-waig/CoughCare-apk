# Frontend Environment Variables Example
# Copy this file to .env and adjust values as needed
# Vite requires variables to be prefixed with VITE_ to be exposed to the client

# Backend API Base URL
# For development: http://localhost:5001 (or the port your backend is running on)
# For production: https://your-production-api-domain.com
# Leave empty to use relative paths (useful when proxying through nginx)
VITE_API_BASE_URL=http://localhost:5001

# Logo Configuration (Optional)
# VITE_LOGO_VERSION=1
# VITE_LOGO_ALT=AI Cough Screening Assistant
